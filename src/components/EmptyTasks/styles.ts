import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    emptyContainer: {
        padding: 24,
        alignItems: "center",
        gap: 16,
    },

    text: {
        fontSize: 16,
        color: "#808080",
        fontWeight: "bold"
    },

    subText: {
        fontSize: 16,
        color: "#808080",
    }
})