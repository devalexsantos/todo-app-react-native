import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    separatorContainer: {
        flexDirection: "row",
        padding: 24,
        alignItems: "center",
        justifyContent: "center"
    },
    separator: {
            width: "100%",
            borderWidth: 1,
            borderColor: "#333333",
    }
})